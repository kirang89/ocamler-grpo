export PYTHONUNBUFFERED=1
export TRAINING_DATASET=kiranpg/ocaml-training-problems
export GRPO_NUM_GENERATIONS=6
# Slight increase from 0.8 to 0.9 for modestly more exploration
# The reward fix (0 for END-only) should do most of the work
export GRPO_TEMPERATURE=1.0
# Increased from 512 to 704 to fit 3 examples (305 tokens) + longest problems (386 tokens)
# Ensures 100% of problems fit without truncation
export GRPO_MAX_PROMPT=704
export GRPO_MAX_COMPLETION=600

